### from selenium import webdriver# from selenium.webdriver.common.by import By# from selenium.webdriver.support.wait import WebDriverWait# from selenium_stealth import stealth# from selenium.webdriver.support import expected_conditions as EC## test_url = ''## options = webdriver.ChromeOptions()# options.add_argument('--no-sandbox')# options.add_argument('--disable-dev-shm-usage')## driver = webdriver.Chrome(options=options)# stealth(driver,#         languages=["en-US", "en"],#         vendor="Google Inc.",#         platform="Win32",#         webgl_vendor="Intel Inc.",#         renderer="Intel Iris OpenGL Engine",#         fix_hairline=True,#         )# wait = WebDriverWait(driver=driver, timeout=10)# driver.get('https://comfy.ua/ua/search/cat__963/?q=wacom')### elements = wait.until(EC.presence_of_element_located((By.CSS_SELECTOR, '#q-app > div > main > div:nth-child(3) > div > div.category-content.main__content > div.products-catalog > div')))# for item in elements.find_elements(By.CLASS_NAME, 'products-list-item products-catalog-grid__item products-list-item--wide-grid'):#     name = item.find_element(By.CLASS_NAME, 'products-list-item__info').get_attribute('title')#     price = item.find_element(By.CLASS_NAME, 'products-list-item-price__actions-price-current').text#     print(name,price)